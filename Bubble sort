A=map(int,raw_input('Give the numbers to be sorted separated by space\n').split())
length=len(A)
for i in range(length):
    for j in range(0,length-i-1):
        if A[j]>A[j+1]:
            A[j],A[j+1]=A[j+1],A[j]
            print A
print A
